export default function MapPane(){
  // Placeholder shell. Hook up Mapbox/Leaflet later.
  return (
    <div className="surface" style={{height:"100%", minHeight:420, display:"grid", placeItems:"center"}}>
      <div className="fine">Map coming soon (pins, cluster, bounds sync)</div>
    </div>
  );
}
